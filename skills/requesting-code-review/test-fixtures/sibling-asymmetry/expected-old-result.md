### Strengths
- The `Inspect` method follows the same `Send`-with-args pattern as `Create` and `Delete`.
- Code is concise and readable.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
None.

### Assessment

**Ready to merge?** Yes

**Reasoning:** The implementation matches the dispatch. `Inspect` follows the same `client.Send` pattern as its siblings. Looks good. Approved.
